version: '1'
component:
  name: mqtt_button
  vendor: Merce project group
  outputs: 
    - name: toggle_output
      protocol: 
          name: mqtt
          type: binary # or json, protobuf
          topic: /mercedge/toggle_button
          qos: 0
          retain: false
      porpreties:
        - name: output # proprety name is output
          type: boolean
          description: toggle output intferface boolean proprety.
        
        # - name: schema_test_output
        #   schema:
        #     $ref: "#/definitions/Output"
        
  # defintions:

  description: mqtt demo toggle button component.